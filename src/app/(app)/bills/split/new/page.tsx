import QuickBillPrototype from './QuickBillPrototype';

/**
 * Route prototype Fase 0 untuk menguji alur input manual Quick Bill Canvas.
 * State pada halaman ini belum disimpan ke database dan seluruh aksi akhir
 * sengaja hanya memberi feedback lokal.
 * @returns {ReactNode} Prototype interaktif editor Bagi Tagihan.
 */
export default function NewSplitBillPrototypePage() {
  return <QuickBillPrototype />;
}
